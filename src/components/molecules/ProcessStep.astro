---
import type { ProcessStep } from '@/types';

interface Props {
  step: ProcessStep;
}
const { step } = Astro.props;
---

<div class="step fade-up">
  <div class="step-num">{step.num}</div>
  <h3 class="step-title">{step.title}</h3>
  <p class="step-desc">{step.description}</p>
</div>

<style>
  .step {
    text-align: center;
    padding: 0 20px;
    position: relative;
  }
  .step-num {
    width: 56px;
    height: 56px;
    background: var(--gradient-accent);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: var(--font-display);
    font-size: 1.1rem;
    font-weight: 800;
    margin: 0 auto 20px;
    position: relative;
    z-index: 1;
    box-shadow: 0 4px 20px rgba(0, 87, 231, 0.4);
  }
  .step-title {
    font-weight: 700;
    font-size: 1rem;
    margin-bottom: 8px;
  }
  .step-desc {
    color: var(--color-gray);
    font-size: 0.85rem;
    line-height: 1.6;
  }
</style>
