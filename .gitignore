# build output
dist/
# generated types & cache
.astro/
# deps
node_modules/
# env
.env
.env.production
# os / editor
.DS_Store
*.log
# agent skills — seul skills-lock.json est versionné (modèle A).
# Les fichiers + symlinks par agent sont restaurés via `npx skills experimental_install`.
.agents/
.claude/skills/
*.local.json
